<h1>Sign Up</h1>
<%= form_for(@user) do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>
    <%= render 'fields', :field => f %> 
    <div class="actions">
      <%=  f.submit "Sign Up" %>
    </div>
<% end %>
